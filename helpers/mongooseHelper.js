var mongoose = require('mongoose');

// A Schema is a template for a generalized object
// Use this to create a blueprint of your object!
var Schema = mongoose.Schema,
    ObjectId = Schema.ObjectId,




